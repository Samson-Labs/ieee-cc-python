FROM public.ecr.aws/lambda/python:3.13

COPY src/extractors/pptx_extractor_requirements.txt ${LAMBDA_TASK_ROOT}/requirements.txt
RUN pip install --no-cache-dir -r ${LAMBDA_TASK_ROOT}/requirements.txt

COPY src/ ${LAMBDA_TASK_ROOT}/src/

CMD ["src.handlers.pptx_extractor_handler.handler"]
